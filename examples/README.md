# Examples

This folder contains runnable examples for the terminal-model contract.

- `pilates/`: a local, deterministic example that simulates how PILATES would stage inputs, run the container-facing contract, and publish the canonical artifact.

The example is standalone and does not require PILATES itself.
